<template>
  <div class="glass p-8 rounded-3xl flex flex-col gap-6 hover:border-[var(--text)]/30 transition duration-300">
    <div class="w-12 h-12 bg-[var(--text)]/5 rounded-2xl flex items-center justify-center border border-[var(--text)]/5">
      <i data-lucide="{{$icon}}" class="w-6 h-6 text-[var(--text)] opacity-80"></i>
    </div>
    <div>
      <h3 class="text-xl font-bold lowercase tracking-tight text-[var(--text)]">{{$label}}</h3>
      <p class="text-sm opacity-40 leading-relaxed mt-2 text-[var(--text)]">{{$desc}}</p>
    </div>
  </div>
</template>